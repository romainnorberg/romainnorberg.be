### Install

- `cp .env.dist .env`
- add to `/etc/hosts`: 
```
# romainnorberg.be
127.0.0.1       romainnorberg.local
127.0.0.1       resume.romainnorberg.local
127.0.0.1       blog.romainnorberg.local
```

### Local

- `symfony serve`
